#include <stdlib.h>
#include "main.h"

/**
 * _abs - Entry point
 * Description - computes the absolute value of an integer
 * @c: functions accept argument c
 * Return: Always 0
 */

int _abs(int c)
{
	return (abs(c));
}
